/** Address snapshot stored on an order (jsonb), independent of `addresses`. */
export interface OrderAddress {
  fullName: string;
  line1: string;
  line2: string | null;
  city: string;
  state: string;
  postalCode: string;
  country: string;
  phone: string | null;
}
